# Upper levels, standalone — 20 minutes. Beats for ./bin/demo run upper
# Pairs with upper-levels/slides/UPPER-LEVELS.md (18 slides).
#
# MODE: run = the script runs it | copy = clipboard | manual = nothing to run
#
# One live thing in the whole demo: beat 4, a deterministic script that takes
# seconds. Everything else is a checkpoint or a capture, because a twenty-minute
# slot has no room for a stage that might take four.
#
# Beats 12-15 are THE CLOSE and are UNCUTTABLE. Cut order when the room runs
# long: beat 5, then 7, then 11, then 3, then 2. Beat 1 never cuts - it is the
# hinge from the keynote, and without it the room is watching a tool demo.
#
# The repo stays on level-5-supervising throughout. Nothing switches branches
# except beat 7, which reads one file out of l4-1-sliced without checking it out.

### Tonight, and the question you already have
TIME: 50s
SAY: SLIDES 1 AND 2. Neither of these ever cuts. Fifty seconds for both.
SAY: The keynote's last slide named this session and signed off with: carpaccio,
SAY: cutting - bring the question. This is that slide, arrived.
SAY: Do NOT re-introduce yourself at length. The room met you this morning.
SAY: One line: this morning I made an argument; tonight it has to survive a
SAY: repository.
SAY: Then slide 2, and say it plainly: you already have the question. If this
SAY: turns out to be wrong, what does it take to put right. Undo, Repair,
SAY: Migrate, Answer.
SAY: I am not going to teach it again. I am going to show you what happened when
SAY: I actually ran it.
SAY: This morning it was an argument. For the next twenty minutes it is a
SAY: measurement.
SAY: Tell them the last slide of this demo is that same slide. Then move.
MODE: manual

### What ships, and what you are about to watch
TIME: 40s
SAY: SLIDE 3. Cut this FIFTH, and only after the other four.
SAY: Two of the five sentinels from the keynote are running tonight: carpaccio
SAY: before the spec, cartographer and diaboli on the spec. Plus the reservoir
SAY: warden, which is not tied to a moment - and which stopped me.
SAY: The Brief Sentinel and the Rehearsal Sentinel are still in build. Say so.
SAY: You said so this morning; saying it again is what keeps this a demo rather
SAY: than a pitch.
SAY: Then the line that matters - dimmed everywhere is not twilight, it is
SAY: theatre. You are about to watch an instrument tell me this repository is
SAY: dimmer than its branch name claims. And I left that in.
MODE: manual

### What is already in the room
TIME: 60s
CMDTIME: 35s
SAY: One minute, no history lesson. This repository already has a harness: six
SAY: constraints, five of them deterministic, three loops - edit time, merge time,
SAY: and a weekly sweep - and a rule that any change under src slash carries a
SAY: decision record.
SAY: Do not explain how it got there. The room did not come for levels one to three.
SAY: CUT THIS BEAT FOURTH if you are over time.
CMD: sed -n '/^## Constraints/,/^### Abstraction/p' HARNESS.md | grep -E '^###|Enforcement|Scope' | head -24
MODE: run

### Watch it catch something — the only live thing today
TIME: 90s
CMDTIME: 40s
SAY: This is a shell script and git. It takes seconds and it cannot wander.
SAY: Say the one sentence and leave it: both of those outputs came from a change
SAY: made by the pipeline I am about to show you.
SAY: Do NOT explain the payoff now. It arrives at beat 9, about four minutes from
SAY: here, and it lands harder because the room already watched the check run.
SAY: It stages a one-line change under src, runs the check, and puts the tree
SAY: back. Nothing is committed and nothing is left behind.
SAY: If it will not run, the captured pair is on the slide and looks identical.
SAY: Say it is a recording.
CMD: printf '\n' >> src/main/resources/templates/owners/findOwners.html && git add src/main/resources/templates/owners/findOwners.html && scripts/check-decision-record.sh --staged; git reset -q && git checkout -- src/main/resources/templates/owners/findOwners.html
MODE: run

### The ceiling Level 3 hits
TIME: 30s
SAY: Everything you just saw bounds what MAY happen to the code.
SAY: None of it has an opinion about what SHOULD happen - whether the feature was
SAY: worth building, which of two designs is better, what to build next.
SAY: It says no. That is the ceiling, and it is why there is a level four.
SAY: CUT THIS BEAT FIRST if you are over time.
MODE: manual

### A pipeline in front of the work, and a person inside it
TIME: 90s
CMDTIME: 60s
SAY: Four hard gates, two soft ones. Every gate is a point where an agent has
SAY: produced something it is forbidden to finish, because finishing it is a
SAY: person's job.
SAY: Point at the enforcement column while you say it: six read human, one reads
SAY: deterministic. That column exists because an instrument caught this file
SAY: claiming enforcement it did not have.
CMD: sed -n '/^| # | Stage/,/^| 5 |/p' PIPELINE.md | $NOTES/bin/fmt-stage-table.py
MODE: run

### pending is the whole gate
TIME: 45s
CMDTIME: 30s
SAY: Five slices. Every disposition reads pending. The agent produced all of them
SAY: and will not fill any of them in.
SAY: Hard gates block on pending. Soft gates report a count. That is the entire
SAY: enforcement - no engine, no state machine, a word in a markdown file that a
SAY: person has to change.
SAY: CUT THIS BEAT SECOND if you are over time - say it over the previous slide.
CMD: git show l4-1-sliced:docs/superpowers/slices/owner-books-visit-against-vet-availability.md | grep -nE 'disposition:|title:' | head -14
MODE: run

### What four gates produced on one real feature
TIME: 90s
CMDTIME: 50s
SAY: One feature: an owner books a visit against a vet's published availability.
SAY: Sixty-two decisions surfaced. Forty disposed. Twenty-two carried knowingly.
SAY: Forty-five minutes of my attention against nineteen hours on the clock - do
SAY: not quote the nineteen hours as work.
SAY: Two hundred and eleven dollars at list price, and nothing actually billed,
SAY: because it ran on a subscription. Both figures or neither.
CMD: ls docs/superpowers/*/ | head -20; echo; grep -c 'disposition:' docs/superpowers/*/*.md | tail -6
MODE: run

### The pipeline bought no exemption — the payoff
TIME: 45s
SAY: Here is the line beat 4 was for.
SAY: That governed, spec-first, four-gate pipeline made a change under src slash
SAY: and the same script you watched at the start refused it. Advisory at commit,
SAY: blocking on the pull request. No agent talked it round.
SAY: A better loop does not get a discount on the rules.
MODE: manual

### The gates recurse
TIME: 60s
SAY: Two objections were accepted. Closing them produced eight more decisions, of
SAY: which four were closed, which produced three more.
SAY: The translation bill went from twenty strings to forty, and the part nobody
SAY: can derive went from ten to twenty.
SAY: No single step was wrong. Every one of them was the right call at the time.
MODE: manual

### Nothing was watching the trajectory
TIME: 45s
SAY: Hard gates block on pending. Soft gates report a count. The harness reads
SAY: markdown and shrugs.
SAY: None of them can see: we are four levels deep in fixing the fixes, at
SAY: quarter to midnight, and the cost is compounding.
SAY: The only thing that can notice is me, and I am the one resource in this
SAY: system that nothing measures.
SAY: CUT THIS BEAT THIRD if you are over time.
MODE: manual

### What is being supervised changes
TIME: 45s
SAY: Everything so far supervised the code. Constraints, gates, tests, review -
SAY: all of it pointed at the artefact.
SAY: What needs supervising now is whether I can still answer for it.
MODE: manual

### What a sentinel is
TIME: 30s
CMDTIME: 20s
SAY: Same plugin. Same file format. One line each.
SAY: No Write. No Edit. No Agent. It cannot change the thing it watches and it
SAY: cannot delegate to something that can.
CMD: cat $NOTES/level-5/trust-boundary.txt
MODE: run

### An instrument found where I would get lost
TIME: 2 min
CMDTIME: 95s
SAY: Read the uncomfortable part aloud, and do not editorialise it away.
SAY: Then: I specified that code. I adjudicated its gates. I approved its plan.
SAY: I did not write a line of it.
SAY: Then the bridge: the sentinel that watches me was asked whether it can tell
SAY: a gate on a typo from a gate on consequential work. It said it cannot, and
SAY: that it would not pretend otherwise.
SAY: OPTIONAL, 20s, only if you are ahead - default is to cut it: they can also
SAY: be quietly wrong. The one command that authors a pact sources a library that
SAY: finds itself through BASH_SOURCE, which zsh does not set. Under bash the
SAY: pact reads declared; under zsh, malformed. Both return success. Its own
SAY: header predicts that failure and its tests run under bash. Issue 617.
SAY: Land the category, not the plugin. Never a story about someone's bug.
CMD: sed -n '28,45p' $NOTES/level-5/diagnosis.yaml
MODE: run

### THE CLOSE — the descent. Terminal off from here.
TIME: 90s
SAY: TERMINAL OFF. One prepared slide.
SAY: Twelve characters of prototype text in a Thymeleaf template, replaced at
SAY: runtime. The rendered page is identical before and after. Nobody has ever
SAY: seen it and nobody ever will.
SAY: Just make the change: under a minute, nothing, no gates.
SAY: Through the habitat: forty-five minutes. Four human gates. Nine agents.
SAY: Fourteen hundred and fifty-seven lines of artefact.
SAY: Fifty-two dollars sixteen at API list price - and nothing actually billed.
SAY: Both figures or neither.
SAY: Let it land. Let the room laugh.
MODE: manual

### Take the laugh away
TIME: 45s
SAY: Say this before the laugh settles.
SAY: Every one of those agents was told that finding nothing was a perfectly good
SAY: answer, and that padding would corrupt the measurement. Not one padded.
SAY: And every single one found something true: a coverage gap. A false claim in a
SAY: record the harness requires. A scope claim wrong under the spec's own
SAY: standard. A standard that collides with a directive. A standard with no
SAY: defined answer at all. And a question nobody had asked.
SAY: A reviewer would want all six.
SAY: The ceremony was proportionate. The findings were real. And it still cost
SAY: fifty-two dollars to fix a string nobody can see.
SAY: If the room leaves thinking the pipeline is stupid, this close has failed.
MODE: manual

### The turn
TIME: 45s
SAY: So the answer is not to build the habitat higher.
SAY: A habitat that can only work at its highest setting is a badly tuned habitat.
SAY: What you have just watched is not a ladder to the top. It is a repertoire,
SAY: and the skill is choosing the mode the work deserves.
SAY: Deliver it as a conclusion, not an apology.
MODE: manual

### The question, then the repository. Then stop.
TIME: 60s
SAY: One question picks the mode: if this turns out to be wrong, what does it take
SAY: to put right?
SAY: Undo. Repair. Migrate. Answer.
SAY: The typo is Undo. Revert it and it never happened. It got four gates and
SAY: fifty-two dollars.
SAY: The booking feature is Migrate - NOT NULL in three dialects, rows rewritten,
SAY: packages coupled. And it is also Answer: the owner's page shows a named
SAY: clinician and a time beside "your visit has been booked", and nothing in the
SAY: clinic agreed to either. No migration fixes that. Someone has to be told.
SAY: Ask only "can I revert it" and you get Migrate. True, and not the whole answer.
SAY: The class that picks the mode is the most expensive one the work touches.
SAY: Last slide: the repository. Go and look. Then ask the question.
SAY: Then stop talking. Do not end on the plugin.
MODE: manual
